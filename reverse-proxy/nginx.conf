#  upstream backend {
#     server server1:5000;
#     server server2:5001;
#     server server3:5002;
#  }

#  server {
#     listen 80;
#     server_name localhost; 
#     location / {
        
#         limit_req zone=ratelimit burst=20 nodelay;

#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host $host;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "Upgrade";
#         proxy_pass http://backend;
#         proxy_http_version 1.1;
#     }
#  }




👇👇👇This only serves the backend by load balancing
I ran into an issue with the containers deployed on linux server and i couldnt access the frontend via nginx.
# worker_processes 1;

# events { worker_connections 1024; }

# http {
#     upstream backend {
#         server server1:5000;
#         server server2:5000;
#         server server3:5000;
#     }

#     server {
#         listen 80;
#         server_name localhost;

#         location / {
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header Host $host;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "Upgrade";
#             proxy_pass http://backend;
#             proxy_http_version 1.1;
#         }
#     }
# }



worker_processes 1;

events {
    worker_connections 1024;
}

http {
    upstream backend {
        server server1:5000;
        server server2:5000;
        server server3:5000;
    }

    server {
        listen 80;
        server_name localhost;

        # Serve React frontend
        location / {
            try_files $uri $uri/ /index.html;
            root /usr/share/nginx/html;
        }

        # Proxy API requests
        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}

